import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Communication OSCE course - Extra roleplay package",
  description: "Comprehensive preparation with 16 live roleplay sessions — double the practice, double the confidence. Covers every station type with extended feedback.",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
