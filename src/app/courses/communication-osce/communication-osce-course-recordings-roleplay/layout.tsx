import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Communication OSCE course - Recordings & Roleplay package",
  description: "Self-paced access to recorded sessions plus future batch roleplay participation. Ideal for candidates who need flexibility.",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
